{
  "name": "soundx-quality-root",
  "version": "1.0.0",
  "private": true,
  "description": "SoundX Quality - Root workspace scripts",
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev"
  }
}
